module.exports = {
    content: [
      "./index.html",      // Include the path to your HTML files
      "./src/**/*.{js,jsx,ts,tsx}",  // Include the path to your JavaScript/TypeScript files (if applicable)
      // Add more paths as needed (e.g., for React or Vue files)
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }